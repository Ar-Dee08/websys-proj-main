login credentials to use:
admin@email.com
12345678

reminders:
- user @email.com for the email for formality

color palette inspo:
https://colorhunt.co/palette/e4e0e1d6c0b3ab886d493628

git commands:
"git remove -v" to check the origin and main repos
"git fetch upstream" fetch the latest changes from upstream
"git checkout main" local main branch
"git merge upstream/main" merge the changes from the upstream to main
"git push origin main" push the updates to your remote fork
"git add <file name>" resolving conflicts, stage the changes
"git commit -m "Resolved Merge Conflicts"" commit the changes
"git push origin main" push the resolved origin main
"git pull origin main" synch your main with the remote repo

to do:
// dashboard
- show the count and total amount of the orders depends on the successful checkouts
// products
- add edit product for updates
- add to cart feature
- add cart page
- in the cart page, checkbox for the selected products to checkout
- checkout feature, it will show the total amount and a success message
// admins
- tables for admin log
- can be able to edit or delete
// customer
- *is this necessary?*
// profile
- fix the edit profile feature

// frontend part
- add styling (external, internal, inline css)